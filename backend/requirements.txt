# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.36
psycopg[binary]
alembic==1.14.1

# Redis
redis==5.2.1

# Data processing (unpinned — exact versions lack Python 3.14 wheels)
pandas>=3.0
numpy>=2.4
scipy>=1.17
statsmodels>=0.14.6

# Data sources
fredapi==0.5.2
yfinance==0.2.51

# CORS and validation
pydantic>=2.10

# ML inference (adversarial bandit — torch installed via Dockerfile for CPU-only wheel)
gymnasium==0.29.1
networkx>=3.2

# Testing
pytest==8.3.4
httpx==0.28.1
anthropic==0.49.0
